# Frontend environment (copy to rehoboth/.env for local overrides)

# API base URL (required)
VITE_API_URL=http://localhost:5000/api

# Public site URL (optional — defaults to window.location.origin in the browser)
# VITE_SITE_URL=https://rehobothhealthmassage.com

# Smartsupp live chat: configured in rehoboth/index.html (not via env var)
